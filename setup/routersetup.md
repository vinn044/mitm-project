# Router / Gateway Setup

- VirtualBox default gateway used as the router
- Gateway IP address: `10.0.2.2`
- Both VMs were connected to the same virtual network through this gateway

The gateway was used to route traffic between the victim machine and the internet during the attack.